# SagaCrypto / WPM — Sitemap & Update Log

> Dokumen ini adalah peta situs (frontend + admin panel) sekaligus riwayat
> perubahan proyek WPM/SagaCrypto. **Dokumen ini akan terus diperbarui**
> setiap kali ada perintah/prompt baru yang mengubah struktur menu, route,
> atau modul — jadi selalu cek bagian "Update Log" di bawah untuk tahu apa
> yang terakhir berubah.

Terakhir diperbarui: **18 Juli 2026**

---

## 1. Peta Situs Publik (Frontend — sagacrypto.com)

**Clean URL sejak 13 Jul 2026** lewat `.htaccess` (root) — file `.php` &
query string aslinya tetap jalan langsung (backward-compatible, gak ada
link lama yang putus), tapi semua link internal & canonical tag sekarang
pakai bentuk clean-nya. `.htaccess` ditulis portable (gak pakai
`RewriteBase` absolut) supaya otomatis jalan baik di local (folder/subfolder
apa pun) maupun online (root domain) tanpa perlu diubah manual.

| Route (clean) | Route (asli, tetap jalan) | File | Keterangan |
|---|---|---|---|
| `/` | `/index.php` | `index.php` | Homepage — urutan: hero berita utama, section Featured/Pamungkas (dinamis dari admin), widget Crypto/Market, Latest News, Trending, Tentang Kami, App Promotion (placeholder — modul belum aktif), Kontak (section Newsletter dihapus 18 Jul 2026, lihat Update Log) |
| `/kategori` | `/kategori.php` | `kategori.php` | Semua berita (menu "Berita"), paginated |
| `/kategori/<slug>` | `/kategori.php?slug=<slug>` | `kategori.php` | Berita per kategori artikel |
| `/tag/<slug>` | `/kategori.php?tag=<slug>` | `kategori.php` | Berita per tag artikel |
| `/artikel/<slug>` | `/artikel.php?slug=<slug>` | `artikel.php` | Detail artikel — sticky sidebar ads, ad slot above/middle/below-article, related articles, share button, view counter |
| `/crypto` | `/crypto.php` | `crypto.php` | Halaman penuh harga crypto (live via Crypto API), auto-refresh |
| `/pencarian?q=<kata kunci>` | `/pencarian.php?q=<kata kunci>` | `pencarian.php` | Pencarian publik ke artikel published |
| `/contact-submit.php` | — | `contact-submit.php` | Handler POST form kontak/newsletter (tidak punya halaman sendiri, redirect balik ke `index.php#kontak`) |
| `/sitemap.xml`, `/sitemap-index.xml` | `/sitemap.php?type=index` | `sitemap.php` | Sitemap index — daftar sub-sitemap di bawah ini |
| `/sitemap-pages.xml` | `/sitemap.php?type=pages` | `sitemap.php` | Homepage + halaman statis (Semua Berita, Crypto) |
| `/sitemap-articles.xml` | `/sitemap.php?type=articles` | `sitemap.php` | Semua artikel published & included |
| `/sitemap-categories.xml` | `/sitemap.php?type=categories` | `sitemap.php` | Kategori + tag artikel |
| `/sitemap-custom.xml` | `/sitemap.php?type=custom` | `sitemap.php` | URL custom yang ditambahkan manual dari admin |

**Shared includes** (dipakai semua halaman di atas):
- `includes/site-bootstrap.php` — koneksi DB, helper (`wpm_esc`, `wpm_icon`, ad renderer, dst), plus builder clean-URL: `wpm_url_artikel()`, `wpm_url_kategori()`, `wpm_url_tag()`, `wpm_url_crypto()`, `wpm_url_pencarian()` — semua link internal wajib lewat fungsi ini, jangan tulis manual `artikel.php?slug=...` lagi
- `includes/site-header.php` — `<head>` + navbar + Live Ticker bar (render ad slot Header & Below Main Menu)
- `includes/site-footer.php` — footer + ad slot Footer/Popup/Sticky Bottom Mobile

---

## 2. Peta Menu Admin Panel (cms-admin/)

Urutan sidebar terbaru (berlaku sama di desktop & mobile, satu markup):

1. **Dashboard** — `dashboard.php`
2. **Site Settings** — `site-settings.php`
3. **About** — `about-settings.php` (mengatur konten section "Tentang Kami" di homepage)
4. **Banners** — `banners.php`
5. **Featured Content** — `featured-content.php` (builder Pamungkas homepage)
6. **Media Library** — `media-library.php`
7. **Contact Messages** — `contact-messages.php`
8. **Pages & Articles** *(grup)*
    - All Articles — `pages.php`
    - Add Article — `pages.php#create-page`
    - Article Categories — `article-categories.php`
    - Article Tags — `article-tags.php`
    - Authors — `admins.php`
9. **SEO Settings** *(grup)*
    - SEO Dashboard — `seo-dashboard.php`
    - SEO Redirects — `seo-redirects.php`
    - Sitemaps — `sitemaps.php` (+ `sitemap-history.php` untuk Update History, diakses lewat tombol di halaman Sitemaps, bukan menu sidebar terpisah)
    - SEO Schema — `seo-schema.php`
10. **Integrations** *(grup)*
    - Crypto Dashboard — `crypto-dashboard.php`
    - Coin Settings — `coin-settings.php`
    - Crypto API — `crypto-api.php`
11. **Advertisements** *(grup)*
    - All Ads — `ads.php`
    - Add New Ad — `ads.php#create-ad`
    - Ad Positions — `ad-positions.php`
    - Ad Settings — `ad-settings.php`
    - Ad Statistics — `ad-statistics.php`
12. **AI Management** *(grup)*
    - AI Sandbox — `ai-sandbox.php`
    - Prompt Control — `prompt-control.php`
    - AI Credentials — `ai-credentials.php`
    - AI Models — `ai-models.php`
    - AI Agent Settings — `ai-agent-settings.php`
    - Growth Agent — `growth-agent.php`
    - GSC Settings — `gsc-settings.php` (superadmin-only — lihat Update Log 18 Jul 2026)
    - Admin Users — `admins.php`

**Urutan grup sidebar (14 Jul 2026):** Pages & Articles → SEO Settings → Integrations → Advertisements → AI Management — sama persis di desktop & mobile drawer (satu markup, `cms-admin/includes/sidebar.php`).

**Menu yang sudah dihapus (Fase 1):** Products, Product Categories, Product Tags, Product Images, Gallery — tidak ada lagi di sidebar maupun route manapun.

**Menu yang dihapus dari sidebar (13 Jul 2026):** Testimonials — dianggap tidak relevan untuk website tema berita. File `cms-admin/pages/testimonials.php` & datanya masih ada (tidak dihapus), cuma link menu-nya yang disembunyikan.

**Menu yang dihapus dari sidebar (14 Jul 2026):** Special Pages — fitur dianggap belum dipakai, dihapus penuh dari admin (lihat Update Log di bawah untuk detail lengkap).

**Menu yang dihapus dari sidebar (15 Jul 2026):** Matches, League Settings, Livescore API — seluruh modul Livescore Sepak Bola dihapus total dari project (admin, frontend, DB), akan dibangun ulang sebagai project terpisah. Lihat Update Log di bawah untuk detail lengkap.

---

## 3. Modul & Status

| Modul | Status | Catatan |
|---|---|---|
| Articles / Pages | ✅ Aktif | Kategori (12 default), tags, author, SEO fields, featured/trending, view counter |
| Advertisements | ✅ Aktif | 11 posisi default, tracking impression/klik otomatis di frontend |
| Featured/Pamungkas builder | ✅ Aktif | 8 tipe konten (manual/latest/trending/category/crypto/ad/app promo android-ios) — tipe "Data dari Livescore API" dihapus 15 Jul 2026 |
| Crypto API | ✅ Aktif | Default provider CoinGecko, provider bisa diganti dari admin, cache + fallback |
| Live Ticker | ✅ Terintegrasi, **nonaktif default** | Bar harga site-wide, polling `crypto-ticker-data.php` tiap 30 detik (reuse cache Crypto API/CoinGecko — dulu WebSocket Binance langsung, diganti karena domain Binance diblokir ISP di Indonesia). Aktifkan + atur simbol di Crypto API settings |
| Livescore (Sepak Bola) | ❌ **Dihapus total (15 Jul 2026)** | Seluruh modul (Matches, League Settings, Livescore API, halaman/widget/menu frontend, 3 tabel DB) dihapus — akan dibangun ulang sebagai project terpisah. Lihat Update Log |
| App Promotion (Android/iOS) | ⏸️ **On hold** | Belum dikerjakan atas permintaan user — placeholder statis di homepage |
| Dashboard widgets | ✅ Aktif (disederhanakan) | Ad stats card dihapus sementara (belum diperlukan); Livescore API status card dihapus permanen bareng modulnya (15 Jul 2026) |
| Sitemaps | ✅ Aktif | `sitemap_urls`/`sitemap_changelog`/`sitemap_settings`, hook otomatis dari Articles/Categories/Tags/Redirects, XML index + 4 sub-sitemap di `/sitemap.xml` |

---

## 4. Update Log

- **Fase 1** — Hapus modul Products & Gallery (admin, dashboard, search, SEO dashboard, frontend) tanpa hapus data DB.
- **Fase 2** — Articles module: kategori, tags, author, SEO fields, featured/trending flag, view counter, preview page.
- **Fase 3** — Advertisement management: CRUD ads, posisi, settings, statistik, endpoint tracking publik.
- **Fase 4** — Featured/Pamungkas content builder (homepage section builder, 9 tipe konten).
- **Fase 5** — Crypto API integration (provider-agnostic, cache, fallback, error log).
- **Fase 6** — Livescore API integration (API-Football/RapidAPI) — digabung ke grup sidebar "Integrations" bareng Crypto atas permintaan user.
- **Fase 8** — Sidebar restructure & dashboard widgets baru.
- **Fase 9** — Frontend jadi multi-halaman (bukan one-page): homepage, artikel, kategori, crypto, livescore, pencarian. Semua 11 ad slot mulai benar-benar dirender.
- **13 Jul 2026** — Livescore diberi toggle show/hide di frontend (default hidden). Sidebar admin direstruktur: Pages & Articles → SEO Settings (Dashboard+Redirects+Schema jadi satu grup) → AI Management → Integrations → Advertisements.
- **13 Jul 2026** — Dashboard: card "Active advertisements", "Ad impressions", "Ad clicks", "Livescore API status" dihapus sementara (belum diperlukan).
- **13 Jul 2026** — Live Ticker: widget harga crypto real-time via Binance WebSocket (client-side, tanpa server polling), tampil sebagai bar di bawah navbar di semua halaman. Toggle on/off + daftar simbol diatur dari Crypto API settings (nonaktif secara default).
- **13 Jul 2026** — Live Ticker & Breaking News digabung jadi satu bar (dulu dua bar terpisah bertumpuk) — Live Ticker di kiri, Breaking News di kanan, dipisah garis vertikal. Breaking News sekarang tampil site-wide (dulu cuma di homepage).
- **13 Jul 2026** — Bugfix: Live Ticker gak konek (stuck di "…") karena hardcode port 9443 yang sering diblokir firewall/jaringan. Sekarang coba port 443 dulu (port standar HTTPS, paling jarang diblokir), otomatis pindah ke 9443 kalau 443 gagal dalam 6 detik, gantian terus sampai konek.
- **13 Jul 2026** — Homepage direorder: Berita Utama (hero) → Featured/Pamungkas → **Market/Harga Crypto Hari Ini** → Latest News/Berita Terbaru → Trending → sisanya tetap sama. Section Crypto dipindah naik dari posisi setelah Latest News jadi sebelumnya (di atas Latest News), ad slot `between-article-cards` ikut pindah supaya tetap berada di antara dua section tersebut.
- **13 Jul 2026** — Menu "Testimonials" dihapus dari sidebar admin (dianggap tidak relevan untuk website tema berita). Halaman & data testimonial tidak dihapus, cuma link menu-nya.
- **13 Jul 2026** — Live Ticker diganti total arsitekturnya: dari WebSocket Binance langsung (client-side) jadi polling `crypto-ticker-data.php` (endpoint baru, server-side, reuse cache Crypto API/CoinGecko yang sama dengan tabel market) tiap 30 detik. Penyebab: dikonfirmasi domain `binance.com` (termasuk subdomain stream-nya) diblokir Kominfo/Bappebti di level ISP Indonesia sejak pertengahan 2024 — bukan soal port atau firewall lokal, jadi fix port 443/9443 sebelumnya gak akan pernah berhasil. Format pengaturan simbol di admin (Crypto API → Live Ticker) tetap sama (BTCUSDT dst.) supaya gak perlu migrasi data.
- **13 Jul 2026** — Restyle mini-ticker hero homepage (`.ticker-chip`/`.crypto-hero__ticker` di `assets/css/site.css`) dari pill/kotak jadi stat-strip minimalis — lalu di-revert lagi ke pill/kotak (border + background glass) semula atas permintaan user.
- **13 Jul 2026** — Clean URL diimplementasikan lewat `.htaccess` baru di root (portable, gak perlu `RewriteBase` beda antara local/online): `/artikel/<slug>`, `/kategori`, `/kategori/<slug>`, `/tag/<slug>`, `/crypto`, `/livescore`, `/pencarian?q=...`. URL `.php?param=` lama tetap jalan langsung (gak ada redirect paksa, jadi gak ada link lama yang putus), tapi semua link internal + canonical tag di seluruh frontend (index, artikel, kategori, pencarian, crypto, livescore, header, footer) sekarang generate lewat fungsi baru `wpm_url_artikel()`/`wpm_url_kategori()`/`wpm_url_tag()`/`wpm_url_crypto()`/`wpm_url_livescore()`/`wpm_url_pencarian()` di `includes/site-bootstrap.php`. **Perlu dicek manual**: mod_rewrite harus aktif + `AllowOverride All` (atau minimal `FileInfo`) di Apache local (Docker) maupun hosting online — gak bisa dites otomatis dari sandbox ini.
- **13 Jul 2026** — Bugfix: CSS/JS gak konek di URL clean (`/artikel/<slug>` dkk) karena URL jadi "lebih dalam" satu segmen dibanding `/artikel.php` lama, jadi semua asset path relatif (`assets/css/site.css`, dst) salah resolve. Fix: tambah `<base href="...">` di paling atas `<head>` (`includes/site-header.php`), dihitung otomatis lewat fungsi baru `wpm_base_path()`/`wpm_base_href()` di `includes/site-bootstrap.php` (deteksi subfolder app dari `$_SERVER['SCRIPT_NAME']`, jadi tetap benar baik di local subfolder `/wpm/` maupun online di root domain). Sekaligus fix path gambar upload (`/uploads/...`) yang juga sempat berpotensi salah di local subfolder — `wpm_image()` sekarang otomatis nambahin prefix base path yang sama.
- **13 Jul 2026** — **Fase 10 selesai**: migrasi SQL formal. Nambah 5 file baru di `cms-admin/migrations/` (`003_articles_categories_tags.sql`, `004_advertisements.sql`, `005_featured_sections.sql`, `006_crypto_api.sql` — termasuk kolom Live Ticker, `007_livescore_api.sql` — termasuk kolom `show_on_frontend`), mengikuti gaya file `001`/`002` yang sudah ada (idempotent `IF NOT EXISTS`/`INSERT IGNORE`, aman dijalankan berkali-kali). Plus `cms-admin/migrations/README.md` menjelaskan cara pakai serta tabel yang TIDAK bisa direkonstruksi dari kode (pages/media_library/gallery base structure, admins, site_settings, banners, testimonials, seo_redirects, contact_messages, special_pages, prompt_control — ini pre-existing di DB, perlu `mysqldump` asli kalau mau setup database kosong dari nol). Auto-migration PHP yang sudah ada (`cms_ensure_table`/`cms_ensure_column`) TETAP jalan seperti biasa — file SQL ini tambahan buat fresh-install/dokumentasi/disaster-recovery, bukan pengganti.
- **13 Jul 2026** — User upload dump database aktual (`wpm_cms`, 38 tabel). Verifikasi silang: migrasi 003-007 cocok 100% dengan struktur live. Nambah `000_base_schema.sql` — akhirnya melengkapi SEMUA tabel yang sebelumnya "tidak bisa direkonstruksi dari kode": `admins`, `pages` (base), `media_library` (base), `gallery` (base), `site_settings`, `banners`, `testimonials`, `seo_redirects`, `seo_schema`, `contact_messages`, `special_pages`, `menus`, `landing_sections`, `portfolio`, `services`, `agent_prompts` — sekarang seluruh skema live tercatat formal di `cms-admin/migrations/`. Ketahuan juga `ad_settings` belum pernah ke-create live (admin belum pernah buka halaman Ad Settings) — bukan bug, auto-migration akan jalan begitu halaman itu dibuka pertama kali.
- **13 Jul 2026** — Cleanup Products/Gallery lama: file PHP-nya (`products.php`, `gallery.php`, dll — 9 file yang ditandai pending) **sudah dikonfirmasi tidak ada lagi** di server, gak perlu dihapus lagi. Sisi database: tambah `008_remove_products.sql` (destructive, opt-in, harus dijalankan manual) yang men-DROP `products`, `product_categories`, `product_images`, `product_tags`, `product_tag_map` — dikonfirmasi via grep penuh codebase nol referensi live, cuma nyimpen data dummy sisa (1 baris doang). `gallery` **sengaja tidak di-drop** — beda dari products, itu tabel galeri foto general-purpose (bukan cuma foto produk), datanya kosong, bisa berguna lagi nanti; opsi drop-nya ada di komentar dalam file 008 kalau tetap mau dihapus.
- **13 Jul 2026** — **Fase 11 selesai**: full verification pass + laporan akhir (`LAPORAN-AKHIR.md`, baru). Audit menyeluruh: sintaks PHP semua file, link internal, struktur sidebar, referensi menggantung ke fitur terhapus, wiring `.htaccess`. Ditemukan & diperbaiki: Testimonials masih muncul di hasil pencarian admin (`cms-admin/actions/search.php`) meski sudah disembunyikan dari sidebar — dihapus dari query pencarian; placeholder teks search box & komentar `admin.js` yang masih menyebut "testimonials" — diperbarui. File sisa yang dihapus (dikonfirmasi nol referensi): `cms-admin/data/sample-data.php` + `cms_sample_data()` (data dummy pre-pivot), `cms-admin/pages/media-library.php.bak`, `cms-admin/migrate-media-library.php`, `cms-admin/migrate-ai-management.php` (script migrasi satu-kali, sudah self-ditandai "hapus setelah dipakai", migrasinya sudah terekam formal di folder `migrations/`).
- **13 Jul 2026** — Fase 11 juga nemu "Landing Page" (`landing-page.php`) ternyata datanya gak pernah dibaca/ditampilkan di frontend manapun (sisa dari sebelum rebrand, waktu situs masih "TheAwsoft"). Atas keputusan user, **direpurpose jadi halaman "Tentang Kami"** (`cms-admin/pages/about-settings.php`, file lama dihapus): form tunggal buat edit judul + deskripsi utama section Tentang Kami di homepage, plus judul + deskripsi 4 kartu fitur (Berita Crypto/Edukasi Blockchain/Analisis Market/Livescore) — ikon tiap kartu tetap fixed per posisi, cuma teksnya yang bisa diubah dari admin. Tabel `landing_sections` dipakai lagi (page_key='about', section_key='main'/'feature_1'..'4') alih-alih bikin tabel baru. `index.php` diupdate: bagian Tentang Kami sekarang baca dari `landing_sections` (dulu dari tabel `pages` slug='about' untuk judul/body + array hardcode buat 4 kartu fitur). Sidebar & dashboard quick-action ikut diupdate ("Landing Page" → "Tentang Kami").
- **13 Jul 2026** — Label menu admin buat halaman di atas diganti lagi dari "Tentang Kami" jadi **"About"** (sidebar, page title, breadcrumb, heading panel) atas permintaan user — biar gak kepanjangan/redundan sama isi form yang memang isinya soal section "Tentang Kami". Konten frontend (nav menu, anchor `#tentang`, section-kicker, judul default "SagaCrypto, Portal Informasi Crypto Terpercaya") **tidak berubah**, tetap pakai teks "Tentang Kami" — yang berubah cuma label halaman admin-nya.
- **13 Jul 2026** — Artikel seed lama "Why Every Business Needs a Website" (sisa pre-pivot, masih tampil sebagai hero/"Berita Utama" homepage karena satu-satunya artikel published) diganti isinya jadi artikel crypto asli: "Apa Itu Cryptocurrency? Panduan Lengkap untuk Pemula di 2026" (kategori Guides, ~900 kata — definisi, cara kerja blockchain, jenis-jenis koin, kondisi pasar terkini per pertengahan 2026, cara mulai investasi dengan aman, risiko). Data update disiapkan sebagai `content-update-hero-article.sql` di root (bukan migrasi skema, jadi sengaja tidak masuk folder `cms-admin/migrations/`) — perlu dijalankan manual sekali via phpMyAdmin/mysql client karena sandbox ini tidak punya akses DB live.
- **13 Jul 2026** — Ditemukan modul "Banners" (`cms-admin/pages/banners.php`, tabel `banners`) ternyata sama sekali belum disambungkan ke frontend (grep penuh codebase: nol referensi di luar `cms-admin/`) — beda dari modul "Advertisements" yang sudah aktif duluan. Atas permintaan user, **disambungkan ke homepage**: section promo banner baru ditaruh tepat di bawah hero (sebelum ad slot `homepage-hero`), menampilkan gambar desktop/mobile + judul/subjudul/tombol CTA opsional, filter `placement='home'` + `is_active=1` + rentang tanggal jadwal. Implementasi: fungsi baru `wpm_banners_active()` dan `wpm_banner_markup()` di `includes/site-bootstrap.php`, section baru di `index.php`, CSS baru `.banner-strip`/`.banner-card` di `assets/css/site.css` (aspect-ratio 1920:900 desktop, 900:1200 mobile — sesuai rekomendasi ukuran yang sudah ada di form admin). Data seed lama "Build Your Digital Foundation with TheAwsoft" (sisa pre-pivot, belum ada gambarnya) akan otomatis tampil begitu diisi gambar dari admin — tidak dihapus, tetap ada di daftar.
- **13 Jul 2026** — Bugfix: form "New banner"/"Edit banner" error `preg_match(): Unknown modifier '['` + "headers already sent" saat submit. Penyebab: dua baris regex validasi `button_url` di `cms-admin/actions/banners-store.php` (dipakai bareng oleh `banners-update.php`) pakai `#` sebagai delimiter, padahal pola di dalamnya juga mengandung karakter `#` literal (buat validasi link anchor `#id` / `/#id`) — jadi PHP salah baca penutup delimiter di tengah pola. Fix: ganti delimiter jadi `%` di dua baris itu.
- **13 Jul 2026** — Ditemukan modul lain yang senasib dengan "Banners": **"Special Pages"** (`cms-admin/pages/special-pages.php`, tabel `special_pages`) — flat page builder WYSIWYG buat campaign/promo/landing route satu-off, juga belum disambungkan ke frontend sama sekali (satu-satunya referensi di luar admin cuma SEO Dashboard, dan itu cuma audit meta title/description, bukan render). 6 data lama (`home`/`about`/`services`/`portfolio`/`articles`/`contact`) jelas sisa company-profile TheAwsoft pre-pivot. Atas permintaan user, **disambungkan ke frontend**: route baru `/halaman/<slug>` → `halaman.php` (file baru), nge-render row `special_pages` dengan `status='published'` dan slug tidak kosong — title, content WYSIWYG (editor TinyMCE yang sudah ada, sudah support sisip gambar dari Media Library, sama seperti editor Artikel — jadi tidak perlu perubahan tambahan buat ini), meta title/description, canonical. Helper baru `wpm_url_halaman()` di `includes/site-bootstrap.php`, rule baru di `.htaccess`. **Sengaja tidak diroute**: row `page_key='home'` (slug kosong, form-nya bilang "Leave empty for home") — kalau ini diroute otomatis ke `/`, bakal nabrak/nimpa homepage asli (`index.php`) tanpa diminta, jadi dibiarkan inert sampai ada keputusan eksplisit soal itu.
- **13 Jul 2026** — Lanjutan Special Pages: ditambahkan **integrasi menu navigasi**. Tiap special page sekarang punya toggle "Tampilkan di menu navigasi utama" + field "Menu order" di form admin — kalau dicentang (dan halamannya `published` + punya slug), otomatis muncul sebagai item menu baru di navbar utama, posisi di antara "Tentang Kami" dan "Kontak" (sesuai referensi posisi yang diminta user). Default OFF per halaman, jadi 6 data lama TheAwsoft **tidak** otomatis nongol di menu live cuma karena kolom barunya sudah ada — admin harus centang manual satu-satu kalau memang mau dipakai. Implementasi: kolom baru `show_in_menu`/`menu_order` di tabel `special_pages` (auto-migrasi via `cms_ensure_column()` + migrasi formal `009_special_pages_menu.sql`), fungsi baru `wpm_special_pages_in_menu()` dan update `wpm_nav_menu()` di `includes/site-bootstrap.php`, kolom "Menu" baru di tabel listing admin biar kelihatan halaman mana saja yang lagi tampil di menu.
- **14 Jul 2026** — **Checkbox global di seluruh admin panel dirapikan.** Sebelumnya tiap halaman (Crypto API/Live Ticker, Ad Settings, Featured Content, Livescore API, Coin Settings, Pages & Articles, Banners) punya `<style>` lokal duplikat `.field--checkbox { padding-top: 24px; ... }` yang jadi penyebab checkbox "ngambang" sendirian tanpa label jelas di ruang kosong. Diganti satu komponen global `.field--checkbox` di `cms-admin/assets/css/admin.css`: kotak checkbox diperbesar ke 22px (dari 16px), satu baris flex `[checkbox] [judul + deskripsi opsional]` dalam card berbatas (border + background lembut), hover/focus-visible/checked state pakai `var(--accent)`, seluruh area `<label>` bisa diklik (native browser behavior, tanpa JS tambahan). Markup checkbox di 7 file (`crypto-api.php`, `ads.php`, `ad-settings.php`, `featured-content.php`, `livescore-api.php`, `coin-settings.php`, `pages.php`, `banners.php`) diupdate ke struktur baru + semua `<style>` lokal duplikat dihapus.
- **14 Jul 2026** — **Deskripsi Live Ticker di Crypto API ditulis ulang** — kalimat lama yang menyebut "Binance diblokir ISP di Indonesia" dihapus dari tampilan admin (info itu cuma relevan sebagai catatan teknis internal, bukan buat ditampilkan ke admin). Live Ticker sekarang punya `.settings-card` sendiri (bordered card) berisi: judul "Live Ticker" + deskripsi baru yang lebih profesional, checkbox "Aktifkan live ticker di frontend" dengan deskripsi singkat, dan field "Simbol aset" dengan label/placeholder/helper text baru (`.field__hint`, kelas baru di `admin.css`) — semua dalam satu card yang rapi, bukan lagi field lepas yang berantakan.
- **14 Jul 2026** — **Fitur "Special Pages" dihapus dari admin panel** — dianggap belum dipakai secara nyata. Dihapus: `cms-admin/pages/special-pages.php` (file, termasuk form add/edit/delete-nya), entri sidebar "Special Pages", dan panel audit "Special Pages" di SEO Dashboard (2 stat card + 1 tabel + link `Edit` yang tadinya mengarah ke halaman yang baru dihapus — kalau tidak dibersihkan bakal jadi broken link). Karena fitur ini baru saja disambungkan ke frontend di entri Update Log tanggal 13 Jul di atas (route `/halaman/<slug>`, integrasi menu navbar), bagian frontend itu **ikut ditarik balik** sebagai keputusan konsisten — dihapus: file `halaman.php`, rule `.htaccess` untuk `/halaman/<slug>`, fungsi `wpm_url_halaman()` dan `wpm_special_pages_in_menu()` di `includes/site-bootstrap.php`, dan `wpm_nav_menu()` dikembalikan ke bentuk semula (tanpa injeksi menu dari Special Pages). Alasan: tanpa admin panel, fitur menu-navbar itu jadi tidak bisa dikelola sama sekali (checkbox togglenya cuma ada di form yang sekarang dihapus) — mempertahankan separuh fitur (frontend jalan, admin hilang) dianggap lebih membingungkan daripada menariknya penuh. **Tabel database `special_pages` (dan kolom `show_in_menu`/`menu_order` dari migrasi 009) SENGAJA TIDAK DIHAPUS** — sesuai instruksi eksplisit user untuk tidak drop tabel tanpa kepastian, dan supaya data lama (6 baris peninggalan TheAwsoft) tidak hilang kalau suatu saat fitur ini mau dihidupkan lagi. Migrasi `000_base_schema.sql` dan `009_special_pages_menu.sql` juga tidak diubah — tetap sebagai catatan sejarah skema.
- **14 Jul 2026** — **Urutan grup sidebar diubah** jadi: Pages & Articles → SEO Settings → Integrations → Advertisements → AI Management (sebelumnya: Pages & Articles → SEO Settings → AI Management → Integrations → Advertisements). Isi tiap grup & urutan submenu di dalamnya tidak berubah. Satu markup dipakai bareng desktop & mobile drawer (`cms-admin/includes/sidebar.php`), jadi otomatis konsisten di keduanya.
- **14 Jul 2026** — **Modul Advertisements dikembangkan dari image-banner-only jadi 5 format iklan**: Text Ad (baru — gaya "Ad · domain / Headline / Deskripsi / CTA"), Image Banner (perilaku lama dipertahankan 100%), Video Ad (baru — upload/URL, poster, autoplay/muted/loop/controls, autoplay diputar hanya saat elemen kelihatan di viewport via IntersectionObserver), Custom HTML (perilaku lama + sekarang disanitasi otomatis saat disimpan), External Ad Code (baru — dibatasi akun superadmin, tidak pernah disanitasi karena memang buat script embed jaringan iklan tepercaya). Tabel `advertisements` nambah 15 kolom baru (semua nullable/berdefault aman — lihat `cms-admin/migrations/010_advertisements_multi_format.sql`), enum `ad_type`/`device` diperlebar, `ad_positions` nambah 4 posisi baru (`article-before-title`, `article-after-title`, `sidebar-left`, `sidebar-right`), `ad_settings` nambah `rotation_mode`. Form admin (`cms-admin/pages/ads.php`) dirombak total: field dinamis sesuai tipe iklan, validasi per-tipe, penghitung karakter live, target picker artikel/kategori yang bisa dicari (nama, bukan ID mentah), dan panel Live Preview (mode Desktop/Mobile/Sidebar/Inside-article) yang update otomatis saat mengetik — semuanya dirender di dalam admin, tidak memanggil frontend asli. Sekaligus dibetulkan 2 bug lama: (1) iklan sidebar dobel di halaman artikel (dua `<aside>` yang tadinya sama-sama pakai posisi `sidebar` — sekarang jadi `sidebar-left`/`sidebar-right`, data lama otomatis dipindah ke `sidebar-left`); (2) target device (`desktop`/`mobile`) sebenarnya tidak pernah berfungsi karena semua pemanggil `wpm_render_ad_slot()` selalu mengirim `'all'` — sekarang otomatis mendeteksi device pengunjung lewat `wpm_detect_device()` kalau tidak dikirim eksplisit. Rotasi iklan (kalau lebih dari satu iklan berbagi posisi & prioritas yang sama) sekarang bisa diatur Priority/Random/Sequential dari Ad Settings. Keamanan: `cms_sanitize_ad_html()` baru di `cms-admin/includes/functions.php` (blokir `<script>`/`<iframe>`/event handler/`javascript:` URL) dipanggil otomatis saat simpan Custom HTML; External Ad Code dicek `$_SESSION['cms_admin_role'] === 'superadmin'` di setiap jalur simpan/duplikat/lihat, dan disembunyikan total dari daftar All Ads untuk non-superadmin; semua field Text Ad di-escape (`wpm_esc()`) saat render, tidak pernah dianggap HTML mentah. Migrasi formal: `cms-admin/migrations/010_advertisements_multi_format.sql` (lihat juga `migrations/README.md`).
- **14 Jul 2026** — **Perbaikan layout form Add/Edit Advertisement.** Field "Placement scope" bentrok dengan "Device" karena grid 2-kolom `.form-grid` generik meng-auto-place dua target-picker (artikel/kategori, yang tampil/hilang lewat JS sesuai scope) sebagai item kolom biasa — Device jadi ikut geser posisi tergantung picker mana yang sedang tampil. Fix: `.ad-form-grid` baru (scoped khusus halaman ini, `gap: 24px 20px`, `align-items: start`) menggantikan `.form-grid` generik di form ini saja (halaman admin lain tidak kena dampak); dua target-picker sekarang dibungkus `<div class="ad-target-pickers">` full-width satu baris sendiri di bawah Placement scope, jadi Device selalu sejajar konsisten dengan field sesudahnya. Panel Edit form + Live Preview yang tadinya pakai inline `style` tanpa breakpoint sekarang jadi class `.ad-edit-layout` (collapse ke 1 kolom di ≤1100px) — inilah penyebab "antar-form saling menabrak" di layar sempit. Tidak ada perubahan pada logika simpan (create/update/delete/toggle/duplicate).
- **14 Jul 2026** — **Modul baru: Sitemaps** (`SEO Settings → Sitemaps`). Mengelola seluruh URL yang masuk sitemap, mencatat setiap perubahan konten, dan menyediakan XML sitemap valid untuk mesin pencari — lihat detail lengkap di bawah (bagian ini panjang karena modulnya besar):
  - **Database (migrasi baru `011_sitemaps.sql`)**: 3 tabel baru — `sitemap_urls` (satu baris per URL: loc, content_type, status, included, priority, changefreq, lastmod, last_detected_change, sitemap_file, overrides_json untuk field yang dikunci manual), `sitemap_changelog` (riwayat setiap perubahan — action, content_type/id, old/new URL, changed_fields, triggered_by, result), `sitemap_settings` (singleton — rules_json berisi default priority/changefreq/included per content type, plus last_generated_at/last_success_at/last_error). Juga nambah kolom `pages.noindex` (checkbox baru di form artikel — "Exclude from search engines").
  - **Service terpusat** (`cms-admin/includes/sitemap-service.php`) — bukan cuma tombol manual: setiap kali artikel/kategori/tag/redirect disimpan atau dihapus, hook di halaman admin terkait langsung memanggil service ini untuk meng-upsert baris `sitemap_urls` + mencatat `sitemap_changelog`, jadi sitemap selalu ikut berubah otomatis tanpa perlu klik apa pun. Auto-rule priority/changefreq dipakai selama admin belum mengunci manual field itu di baris tertentu (lewat aksi Include/Exclude atau custom URL).
  - **Hook terpasang di**: `cms-admin/pages/pages.php` (create/update/delete artikel — deteksi publish/unpublish, slug berubah, canonical berbeda dari URL sendiri otomatis dikeluarkan dari sitemap, noindex otomatis dikeluarkan), `article-categories.php`, `article-tags.php` (create/update/delete), `seo-redirects.php` (create/update — kalau `old_url` cocok dengan salah satu URL yang sudah tercatat, baris itu otomatis ditandai status "Redirected" & dikeluarkan dari sitemap).
  - **Halaman admin**: `cms-admin/pages/sitemaps.php` — summary card (Total/Published/Excluded URLs, Sitemap Files, Last Generated, Last Successful Update, Errors), tombol Regenerate Sitemap (full resync dari semua sumber konten + hapus baris yang sumbernya sudah tidak ada), Validate Sitemap (cek URL absolut/priority valid/changefreq valid/tidak ada duplikat, baris bermasalah ditandai status Error), Open Sitemap XML, panel Sitemap Settings (matrix auto-rule per content type), form Add Custom URL, dan tabel semua URL dengan search/filter (content type, status, included/excluded, sitemap file)/sort/pagination, plus aksi View/Edit SEO/Include-Exclude/Regenerate entry/Inspect/Delete (khusus custom). `cms-admin/pages/sitemap-history.php` — log lengkap dengan filter tanggal/action/content type/result/user, detail per-baris expandable.
  - **Endpoint publik**: `/sitemap.xml` & `/sitemap-index.xml` (index — hanya mendaftar sub-sitemap yang punya minimal 1 URL, supaya tidak ada sub-sitemap kosong yang dilaporkan search engine), `/sitemap-pages.xml`, `/sitemap-articles.xml`, `/sitemap-categories.xml`, `/sitemap-custom.xml` — semua dilayani `sitemap.php` (baru, root project) lewat rule baru di `.htaccess`, XML standar `sitemaps.org/schemas/sitemap/0.9` dengan `lastmod` format ISO 8601.
  - **Keputusan desain yang perlu diketahui**: (1) "Static Page" mencakup homepage + hub bawaan (Semua Berita/Crypto/Livescore) — bukan admin-managed pages, karena modul Special Pages sudah dihapus penuh sebelumnya (lihat entri 14 Jul di atas); (2) "Author" tidak dimasukkan — situs ini belum punya halaman arsip penulis publik, jadi memang belum ada apa-apa untuk didaftarkan (sesuai kualifikasi "jika tersedia" di permintaan); (3) Tag digabung ke `sitemap-categories.xml` (bukan file terpisah) karena daftar contoh index di permintaan cuma menyebut pages/articles/categories/custom; (4) "Scheduled" cuma label informasional (draft dengan `published_at` di masa depan) — sistem publish di project ini memang tidak punya cron/queue yang otomatis publish di tanggal tertentu; (5) artikel dengan `canonical_url` yang beda dari URL-nya sendiri otomatis dikeluarkan dari sitemap (praktik SEO standar: sitemap tidak boleh berisi URL non-canonical).

- **14 Jul 2026** — **Audit database: tabel & kolom yang tidak terpakai.** Atas permintaan user ("field atau table yg ga kepake sebaiknya di hapus saja"), dilakukan grep penuh codebase (setiap tabel + setiap kolom di seluruh `cms-admin/migrations/*.sql`, dicek ke seluruh file `.php` di luar folder migrations) untuk cari skema yang benar-benar mati. Ditemukan & disiapkan migrasi baru `cms-admin/migrations/012_cleanup_unused_tables_columns.sql` (destructive, opt-in, harus dijalankan manual — sandbox ini tidak punya akses DB live) yang men-DROP: tabel `menus` (nol referensi, tidak pernah punya halaman admin), `portfolio` (sama), `services` (sama — hit "services" di grep cuma path folder `/services/PromptLoader.php`, bukan tabel ini), `gallery` (sudah lama diketahui kosong & tidak dipakai sejak cleanup produk 13 Jul, sekarang benar-benar di-drop atas instruksi eksplisit kali ini), `special_pages` (sisa fitur yang sudah dihapus 14 Jul di atas — sebelumnya sengaja dipertahankan "jaga-jaga", sekarang di-drop atas instruksi baru); plus 3 kolom mati di tabel `media_library` yang masih aktif: `original_name` (aplikasi pakai `file_name`), `file_size` (sudah digantikan `file_size_kb`), `uploaded_by` (tidak pernah ditulis oleh alur upload manapun di kode ini, termasuk FK-nya ke `admins`). Tabel `000_base_schema.sql`/`009_special_pages_menu.sql` sengaja tidak diubah (tetap catatan sejarah skema, sama seperti pola `008`).

- **14 Jul 2026** — **Live Ticker (harga BTC/ETH/BNB) dijadikan text berjalan** seperti Breaking News di sebelahnya (sebelumnya statis, cuma bisa di-scroll manual/nggak scroll sama sekali kalau muat). `wpm_live_ticker_markup()` di `includes/site-bootstrap.php` sekarang merender daftar chip **dua kali berturut-turut** di dalam `.live-ticker__scroll` (trik yang sama seperti `wpm_breaking_news_markup()` — konten digandakan supaya animasi CSS loop mulus tanpa "patah" di ujung). CSS: `.live-ticker__track` diganti dari scrollable-manual (`overflow-x:auto`) jadi `overflow:hidden` + mask gradient kiri-kanan (pola sama seperti `.breaking-ticker__track`), `.live-ticker__scroll` baru pakai `animation: tickerScroll 20s linear infinite` (reuse keyframe yang sudah ada dari Breaking News, translateX 0 → -50%), plus `prefers-reduced-motion: reduce` dihormati (animasi mati kalau user minta). `assets/js/live-ticker.js` diupdate: `updateChip()` sekarang pakai `querySelectorAll` (bukan `querySelector`) supaya kedua salinan chip per simbol ikut ke-update harga/perubahan-nya secara live, bukan cuma salinan pertama.

- **14 Jul 2026** — **Bugfix + fitur baru di halaman Crypto** (`crypto.php`). User laporan kotak "3 bar hijau raksasa" di bawah judul "Harga Crypto Hari Ini" dikira grafik — ternyata bug: itu ikon status kecil (`wpm_icon('chart')`, dipakai di `.status-banner` buat nunjukin "Data live/cache..."), tapi CSS `.status-banner` nggak pernah dikasih aturan ukuran buat SVG di dalamnya, jadi browser fallback ke ukuran default SVG (~300x150px) alih-alih ikon mini. Fix: tambah `.status-banner svg { width: 18px; height: 18px; }` di `assets/css/site.css` (juga otomatis benerin halaman lain yang pakai `.status-banner`, misal `livescore.php`). Sekaligus, atas permintaan user, ditambahkan **grafik beneran**: bar chart "Top 10 Market Cap" baru di atas tabel harga, pakai data live/cache yang sama dari `cms_crypto_fetch_coins()` (bukan library eksternal — bar CSS polos, tinggi tiap batang proporsional ke `market_cap` real, warna hijau/merah ikut arah 24h change, label value dipendekin pakai helper baru `wpm_format_compact_number()` di `includes/site-bootstrap.php`, misal "$1.25T"/"$215.57B"). CSS baru: `.market-chart` dan turunannya di `assets/css/site.css`.

- **14 Jul 2026** — **Site Settings disambungkan ke frontend** (sebelumnya sama sekali terputus — nama situs/tagline di navbar dan kartu Kontak di homepage semua hardcode "SagaCrypto"/"Segera hadir", terlepas dari apa pun yang diisi admin). Helper baru `wpm_site_settings($pdo)` di `includes/site-bootstrap.php` (cache per-request, resilient/try-catch). `includes/site-header.php` sekarang baca `site_name`/`site_tagline`/`logo_path` dari situ (fallback ke default lama kalau kosong). Kartu Kontak di `index.php` sekarang baca `email`/`whatsapp_number` (otomatis dibikinin link `mailto:`/`wa.me`)/`instagram_url` (terima handle polos kayak `saga.crypto` atau URL penuh, dua-duanya otomatis jadi link `instagram.com/...` yang benar) — tampil "Segera hadir" cuma kalau field-nya beneran kosong di admin. **Logo**: selama admin belum upload logo custom di Site Settings, navbar tetap nampilin badge fallback "SC" polos (sempat dicoba tambah aksen ikon berlian kecil, tapi ditarik lagi atas permintaan user — dianggap kurang pas) — begitu admin upload gambar logo, otomatis kepakai gantiin fallback ini (`.crypto-logo__mark--img`).

- **14 Jul 2026** — **Bugfix tabel "All Sitemap URLs"** (`cms-admin/pages/sitemaps.php`) — kolom URL sebelumnya kolaps jadi satu karakter per baris (bikin seluruh tabel berantakan, tombol aksi ikut kepotong). Penyebab: tabel pakai layout `auto` (default browser) + kolom URL diset `overflow-wrap: anywhere` tanpa lebar minimum — browser menghitung lebar minimum kolom itu mendekati nol (karena boleh patah di antara dua karakter mana pun), jadi seluruh URL yang panjang & tanpa spasi kepaksa patah tiap karakter. Fix: tabel sekarang pakai `table-layout: fixed` dengan lebar persentase tetap per kolom (class baru `sm-col-*`), URL & nama file sitemap ditampilkan satu baris terpotong (`text-overflow: ellipsis`) dengan tooltip native (`title` attribute) buat lihat teks lengkapnya — bukan lagi wrap berantakan ke banyak baris. Sekalian dibetulkan juga baris filter di atasnya (Search/Content type/Status/Inclusion/Sitemap file/Sort by) — opsi teks panjang seperti "Last modified (newest)" sebelumnya meluber keluar kolomnya sendiri dan nabrak field sebelahnya (classic CSS Grid gotcha: item grid nggak mau menyusut di bawah lebar konten aslinya secara default); fix dengan `min-width: 0` di tiap `.field` dalam grid filter itu.

- **14 Jul 2026** — **Bugfix layout SEO Dashboard** (`cms-admin/pages/seo-dashboard.php`) — kartu "Total Pages" tampil sendirian di baris atas, terpisah dari "Pages — Missing Meta Title"/"Pages — Missing Meta Desc" di baris bawahnya, padahal seharusnya sejajar satu baris. Penyebab: markup-nya memang membungkus kartu-kartu itu dalam DUA `<div class="admin-grid admin-grid--stats">` terpisah (bukan cuma soal CSS) — digabung jadi satu grid container. Sekalian ditambah override lebar kolom (`repeat(3, ...)`, class baru `seo-dash-stats`, cuma berlaku di layar lebar ≥1281px) supaya 3 kartu itu ngisi satu baris penuh, bukan cuma separuh dari grid 6-kolom default yang dipakai halaman-halaman lain dengan kartu lebih banyak.

- **15 Jul 2026** — **Fix "Generate SEO/Article/FAQ" 404 di modul Pages/Article** — tombol "Generate SEO/Article/FAQ with Agent SEO" di `cms-admin/pages/pages.php` selalu gagal dengan HTTP 404. Penyebab: JS-nya sudah lama manggil `fetch()` ke `api/seo-generate.php`, `api/article-generate.php`, `api/faq-generate.php`, tapi folder `cms-admin/api/` itu memang belum pernah dibuat sama sekali — bukan bug config/permission. Dibuatkan 4 file baru: `cms-admin/api/seo-generate.php`, `article-generate.php`, `faq-generate.php` (masing-masing resolve model+API key dari `ai_agent_settings`/`ai_models`/`ai_credentials` via agent_key `seo_agent`, panggil `cms_ai_call_provider()` yang sudah ada di `cms-admin/includes/ai-helpers.php`, lalu parse respons JSON dari AI), dan `cms-admin/api/index.php` (403 guard, sama seperti `actions/index.php`). Sekalian ditambah 2 helper baru di `ai-helpers.php`: `cms_ai_resolve_agent()` (resolve agent config + decrypt API key + fallback ke Prompt Control kalau tersedia) dan `cms_ai_extract_json()` (parse JSON dari respons AI meski dibungkus markdown code-fence). Juga dibenerin cara JS-nya nyusun URL fetch — sebelumnya hardcode naik 2 folder (`../../api/...`) yang gampang salah di struktur admin flat seperti punya SagaCrypto; sekarang pakai fungsi baru `cms_api_href()` di `functions.php` (pola yang sama seperti `cms_action_href()` yang sudah ada), jadi otomatis menyesuaikan topologi nested (dev) maupun flat (production).

- **15 Jul 2026** — **Rapikan layout tombol Generate SEO/Article/FAQ** (`cms-admin/pages/pages.php`) — pesan status/error di antara tombol bisa panjang (mis. pesan error API key belum aktif) dan mendorong tombol lain jadi berantakan. Direstruktur: setiap tombol + pesan statusnya jadi satu grup vertikal (`.pg-ai-actions__item`) di dalam satu baris flex (`.pg-ai-actions`), jadi pesan panjang cuma turun ke bawah tombolnya sendiri tanpa mengganggu posisi tombol lain.

- **15 Jul 2026** — **Logo Google Play & App Store di section App Promotion** (`index.php`) — badge "Google Play — Segera Hadir" / "App Store — Segera Hadir" di homepage sekarang ada ikon logonya. Tambah 2 icon key baru (`apple`, `google-play`) di `wpm_icon()` (`includes/site-bootstrap.php`), plus CSS `.app-store-badge`/`.app-store-badge__icon` di `assets/css/site.css`.

- **15 Jul 2026** — **Fix preview logo/gambar ga muncul di admin online** (`cms-admin/pages/site-settings.php`, `cms-admin/pages/banners.php`) — preview Logo/Favicon/OG Image (Site Settings) dan gambar Banner sudah dirender benar oleh server (pakai `app_asset_preview_url()` yang sudah dibetulkan sebelumnya), tapi ada inline `<script>` di kedua halaman itu yang langsung nimpa lagi `<img src>` begitu halaman dimuat, pakai `BASE_URL` mentah (bukan `cms_public_base_prefix()`) — jadi balik lagi ke masalah split-subdomain lama: URL yang dihasilkan nunjuk ke domain admin (wpm.sagacrypto.com) yang ga punya file fisiknya, gambar 404, lalu disembunyikan sendiri oleh `onerror` handler. Local dev ga kena karena admin & frontend satu domain di situ. Fix: `cmsBaseUrl` JS sekarang di-`json_encode()` dari `cms_public_base_prefix()` (sama seperti server-side), bukan `BASE_URL` mentah.

- **15 Jul 2026** — **Modul Livescore Sepak Bola dihapus total** atas permintaan eksplisit user — akan dibangun ulang sebagai project/website terpisah. Cakupan penghapusan lengkap:
  - **File dihapus** (6): `livescore.php` (frontend), `cms-admin/pages/livescore-matches.php`, `cms-admin/pages/league-settings.php`, `cms-admin/pages/livescore-api.php`, `cms-admin/actions/livescore-api-test.php`, `cms-admin/includes/livescore-api.php` (service/helper: fetch, cache, test connection, semua `cms_livescore_*()`).
  - **Admin panel**: 3 menu (Matches, League Settings, Livescore API) dihapus dari grup sidebar "Integrations" (`cms-admin/includes/sidebar.php`) — sisa Crypto Dashboard/Coin Settings/Crypto API tetap utuh. Link "Livescore API settings" di Quick Actions dashboard (`dashboard.php`) dihapus. Opsi "Data dari Livescore API" dihapus dari dropdown tipe konten Featured Content (`featured-content.php`). Opsi "Halaman Livescore" dihapus dari dropdown Placement scope Advertisements (`ads.php`).
  - **Frontend**: nav menu item "Livescore" (desktop+mobile), section widget Livescore di homepage (`index.php`), link "Livescore" di footer (`includes/site-footer.php`), route `/livescore` di `.htaccess`, fungsi `wpm_url_livescore()`/`wpm_mini_livescore_list()` di `includes/site-bootstrap.php`, CSS `.livescore-tabs`/`.match-card*` di `assets/css/site.css` — semua dihapus. Kartu fitur "Livescore" di section Tentang Kami diganti jadi "Live Market Ticker" (icon `flame`) baik di `index.php` maupun default fallback-nya di `cms-admin/pages/about-settings.php` (`ABOUT_DEFAULTS['feature_4']`) — supaya index array 4-kartu tidak pincang. Semua copy/meta text yang menyebut "livescore" (meta title/description homepage, hero lead, footer disclaimer & tagline, dsb) dibersihkan.
  - **Sitemap**: hook Livescore di `cms_sitemap_full_resync()` (`cms-admin/includes/sitemap-service.php`) dihapus — sitemap statis sekarang cuma Homepage/Semua Berita/Crypto.
  - **Database**: migrasi baru `cms-admin/migrations/013_remove_livescore_module.sql` (opt-in, destructive, belum dijalankan — lihat bagian pending di bawah) yang men-DROP `livescore_api_settings`, `livescore_cache`, `livescore_leagues` (3 tabel, dikonfirmasi cuma dipakai modul ini), plus re-point defensif baris `featured_sections.content_type='livescore_api'` → `'latest'` dan `advertisements.placement_scope='livescore'` → `'global'` (biar tidak ada section/iklan lama yang jadi senyap permanen), dan bersihin baris `api_error_log` yang `source='livescore'`. **Sengaja TIDAK disentuh**: definisi ENUM `livescore`/`livescore_api` di skema `ads.php`/`featured-content.php`/migrasi 004/005 lama (biarkan inert, ALTER ENUM di tabel live berisiko tanpa manfaat berarti), tabel `api_error_log` itu sendiri (dipakai bareng Crypto API), dan baris kategori artikel "Livescore" di `article_categories` (fitur tagging artikel yang beda, bukan bagian modul ini).
  - **Crypto sama sekali tidak tersentuh** — Crypto Dashboard, Coin Settings, Crypto API, Live Ticker, widget harga di homepage/`crypto.php`, semuanya diverifikasi tetap utuh (grep konfirmasi nol referensi Livescore tersisa di file-file Crypto).
  - Migrasi `007_livescore_api.sql` (lama) **tidak diedit** — tetap sebagai catatan sejarah skema, cuma ditambah satu baris note di `migrations/README.md` yang mengarah ke `013`.

- **15 Jul 2026** — **User role (Super Admin/Admin/Editor) sekarang benar-benar punya fungsi berbeda** — sebelumnya semua admin yang login py akses identik apa pun role-nya. Atas permintaan eksplisit user, dibuat 3 tier akses:
  - **Editor**: Pages & Articles (All Articles, Add Article, Article Categories, Article Tags), Media Library, SEO Dashboard, Banners. Selain itu tidak bisa diakses.
  - **Admin**: semua yang Editor punya, plus semua fitur lain **kecuali** Admin Users, AI Credentials, dan Crypto API settings (tiga halaman ini menyimpan kredensial/API key mentah) — juga tetap tidak bisa pakai tipe iklan "External Ad Code" (pembatasan lama yang sudah ada di `ads.php`, sekarang beneran jalan — lihat poin bug di bawah).
  - **Super Admin**: akses penuh tanpa batasan, satu-satunya role yang bisa mengelola Admin Users, AI Credentials, Crypto API, dan External Ad Code.
  - Kalau Admin/Editor coba buka halaman yang bukan hak-nya (misal ketik URL langsung), otomatis di-redirect ke Dashboard dengan pesan error — bukan halaman 403 kosong.
  - **Bug ditemukan & dibetulkan sekaligus** (prasyarat sebelum tier di atas bisa jalan benar): dropdown Role di `cms-admin/pages/admins.php` selama ini mengirim nilai `Super Admin`/`Editor`/`Admin` (title case, ada spasi), padahal kolom `admins.role` di database itu `enum('superadmin','admin','editor')` (lowercase, tanpa spasi) — jadi setiap kali admin membuat/mengedit akun lewat form ini, nilai role yang tersimpan **tidak pernah valid**. Ini juga penyebab pembatasan lama "External Ad Code hanya Super Admin" di `ads.php` diam-diam tidak pernah berfungsi (perbandingannya `=== 'superadmin'` tapi nilai session-nya `'Super Admin'`). Fix: dropdown sekarang mengirim value lowercase yang benar (label yang ditampilkan tetap ramah — "Super Admin"/"Admin"/"Editor"), plus validasi server-side tambahan di `admins-store.php`/`admins-update.php` (tolak nilai role yang bukan salah satu dari tiga itu).
  - **Implementasi**: helper baru di `cms-admin/includes/functions.php` — `cms_admin_role()`, `cms_is_superadmin()`, `cms_is_admin_or_above()`, `cms_require_role(array $allowedRoles)` (dipanggil di awal tiap halaman/action yang dibatasi, redirect + flash error kalau ditolak). Dipasang di 22 halaman admin + 5 file action (lihat daftar lengkap di laporan). Sidebar (`cms-admin/includes/sidebar.php`) dan Quick Actions di Dashboard (`cms-admin/pages/dashboard.php`) ikut difilter per role — link ke halaman yang tidak bisa diakses otomatis disembunyikan, bukan cuma diblokir setelah diklik. **Catatan penting**: role tersimpan di session sekali saat login, jadi kalau Super Admin mengubah role seseorang, perubahan itu baru berlaku setelah orang itu logout-login lagi (sama seperti perilaku nama/email yang sudah ada sebelumnya, bukan bug baru).

- **18 Jul 2026** — **Section "Newsletter" (form berlangganan email) dihapus dari homepage** (`index.php`) — 21 baris dihapus, tidak ada baris ditambah. Bukan penambahan fitur/tabel baru. *(Rationale penghapusan tidak tercatat di commit message.)* Dicek: `contact-submit.php` (handler POST form kontak/newsletter, lihat tabel route di atas) tidak diubah oleh commit ini, dan tidak punya logic yang membedakan subject Newsletter vs kontak biasa (insert generik ke `contact_messages`) — tidak ada dead code yang tertinggal di backend.
- **18 Jul 2026** — **`.gitignore` diperluas** (`Update .gitignore: add sensitive config, AI/API credential files`) — 17 baris ditambah, tidak ada perubahan kode aplikasi: `cms-admin/config/app.php` dan `.env`/`.env.local`/`.env.production`/`*.env` masuk daftar config sensitif; `cms-admin/data/GrowthAgent_Architecture.docx`/`.pdf` masuk bagian AI/API credentials; `cms-admin/uploads/` masuk user-uploaded content; `.claude/` masuk editor/tooling; `logs/` masuk bagian Logs; section baru "Dependency folders" (`vendor/`, `node_modules/`).
- **18 Jul 2026** — **Growth Agent × Google Search Console integration.** Rencana arsitektur lengkap di `docs/GSC_INTEGRATION_PLAN.md` (dibaca & di-approve user sebelum implementasi jalan — termasuk dua keputusan eksplisit: TANPA cron sama sekali, dan TANPA Composer/Google API Client Library). Implementasi:
  - **Migrasi baru** `cms-admin/migrations/015_gsc_search_console.sql`: tabel `gsc_settings` (singleton — service account JSON terenkripsi via `cms_ai_encrypt()` reuse dari AI Credentials, property GSC terhubung, status fetch), `gsc_query_data` (cache query/page/date performance dari GSC, dedupe via hash kolom supaya aman di-upsert berulang), plus `ALTER TABLE growth_agent_jobs ADD COLUMN priority ENUM('normal','high')`.
  - **`cms-admin/includes/gsc-api.php`** (baru) — autentikasi service account tanpa SDK (JWT RS256 hand-rolled pakai `openssl_sign()`, tukar token via REST `oauth2.googleapis.com`, query `searchAnalytics` via REST langsung), pola provider-agnostic sama seperti `crypto-api.php`. **Tanpa cron**: `cms_gsc_fetch_if_stale()` dipanggil lazy dari `growth-agent.php` tiap page load (re-fetch kalau data GSC sudah lebih dari 24 jam), `fetch_lookback_days` default 14 hari supaya aman walau halaman jarang dibuka.
  - **`cms-admin/pages/gsc-settings.php`** (baru, superadmin-only — tier sama seperti AI Credentials/Crypto API) — paste service account JSON, test koneksi otomatis saat simpan, pilih property GSC dari daftar yang bisa diakses service account tsb.
  - **3 jenis analisis peluang baru** di `growth-agent-service.php`: (1) CTR rendah pada artikel existing — reuse `job_type='seo_recommendation'` persis (0 perubahan di `seo-recommendation-review.php`, cukup ganti sumber kandidat artikelnya jadi berbasis data GSC); (2) "striking distance" posisi 5-15 → `job_type='gsc_content_optimization'` (baru, agent_key `growth_agent` baru); (3) query volume tinggi tanpa artikel sama sekali → `job_type='gsc_article_idea'` (baru, agent_key sama). Tipe 2 & 3 lewat antrian Approve/Reject generik yang sudah ada di `growth-agent.php` — tidak perlu halaman review baru.
  - **Priority HIGH/Normal** — kolom baru di `growth_agent_jobs`, dihitung otomatis per tipe scan berdasar besaran impressions/kedekatan posisi ke halaman 1, ditampilkan sebagai badge di tabel "Recent jobs". Approve/Reject tetap sama, badge cuma penanda visual urutan perhatian.
  - Tombol baru di `growth-agent.php`: "Refresh Data" (fetch on-demand, action baru `cms-admin/actions/gsc-refresh.php`) dan "Scan GSC Opportunities" (jalankan ketiga tipe analisis sekaligus).
  - Sidebar: entri baru "GSC Settings" (superadmin-only) ditambah di grup AI Management, setelah "Growth Agent" (yang ternyata belum pernah tercatat di § 2 di atas — sudah ditambahkan sekalian).
  - `services/GrowthAgentPromptBuilder.php` dan `cms-admin/pages/seo-recommendation-review.php` — **0 baris berubah**, keduanya sudah generic per `job_type` sejak awal.
- **18 Jul 2026** — **Revisi Growth Agent × GSC: "Prioritized Opportunities"** (menggantikan sebagian alur di entri tepat di atas). Rencana lengkap di `docs/GSC_OPPORTUNITIES_REVISION.md` (referensi UI konkret dari user, di-approve sebelum implementasi). Perubahan inti: scoring peluang (Impact/Effort/Priority/Reason) sekarang **murni SQL/formula, tanpa panggil AI sama sekali** — AI cuma dipanggil belakangan, satu-per-satu, saat admin klik "Generate" di baris yang dipilih.
  - **Migrasi baru** `cms-admin/migrations/016_gsc_opportunities.sql`: tabel `gsc_opportunities` (item page/query, matched_categories, impact/effort score, priority, recommended_agent/action, reason narasi, metrics_json, status open/actioned); `gsc_settings` (+`opportunity_thresholds_json` — **satu tempat** buat semua angka threshold scoring, bukan tersebar di kode, sesuai permintaan eksplisit user); `growth_agent_jobs.priority` dilebarkan dari 2-tier (`normal`/`high`) jadi 3-tier (`low`/`medium`/`high`, default `medium` — migrasi data existing `normal`→`medium` otomatis, plus fungsi `cms_growth_agent_ensure_priority_enum()` buat instalasi yang sudah kadung jalan migrasi 015 versi lama).
  - **`cms_gsc_compute_opportunities()`** (baru, `gsc-api.php`) — dipanggil **otomatis setiap kali fetch GSC selesai** (di akhir `cms_gsc_fetch_and_cache()`), plus tombol manual "Recompute Opportunities". Kategori: `Low CTR` (gap CTR vs rata-rata site di rentang posisi yang sama, dihitung fair per position-bucket), `Zero-click`, `Page-one` (posisi 11-20), `No article` (query tanpa artikel matching sama sekali). Item yang match >1 kategori pakai aksi termurah (Low CTR > Page-one > No article) sebagai `recommended_action` utama.
  - **Tombol bulk "Scan GSC Opportunities" (dari entri sebelumnya) dihapus total**, digantikan tabel **"Prioritized Opportunities"** di `growth-agent.php`: kolom Priority/Item (`(page)`/`(query)`)/Matched Categories/Impact/Effort/Recommended Agent/Reason + tombol Generate per baris. Panel aggregate stats (Clicks/Impressions/CTR/Avg Position + rentang tanggal, catatan lag ~3 hari) dan tabel "Top Queries" juga ditambahkan.
  - **Fungsi bulk lama dihapus/direfactor** di `growth-agent-service.php`: `cms_growth_agent_scan_gsc_ctr_opportunities()` dihapus (Tipe 1 sekarang dispatch langsung ke `cms_growth_agent_run_seo_recommendation_scan()` dengan 1 halaman dari opportunity yang diklik — fungsi shared ini tidak berubah). `cms_growth_agent_scan_gsc_content_optimization()`/`cms_growth_agent_scan_gsc_article_ideas()` (bulk candidate-query + generate jadi satu) dipecah jadi `cms_growth_agent_generate_content_optimization()`/`cms_growth_agent_generate_article_idea()` (generate 1 item saja, dipanggil dari dispatch `generate_from_opportunity` di `growth-agent.php` — seleksi kandidat sudah pindah ke `cms_gsc_compute_opportunities()`).
  - `seo-recommendation-review.php` dan `GrowthAgentPromptBuilder.php` — tetap **0 baris berubah**.
- **19 Jul 2026** — **Growth Agent "Agent Memory"** — pencatatan pola/insight dari data GSC historis (winning topic/keyword, content gap yang berulang), direview admin dulu sebelum dipakai AI. Rencana lengkap di `docs/GROWTH_AGENT_MEMORY_PLAN.md` (di-approve user sebelum implementasi jalan, 5 keputusan desain diimplementasikan persis tanpa revisi).
  - **Migrasi baru** `cms-admin/migrations/017_growth_agent_memory.sql`: tabel `growth_agent_memory` (`insight_type` `winning_pattern`/`content_gap`, title/description narasi terparametrisasi bukan AI, `supporting_data_json` bukti angka, status `pending_review`/`active`/`archived`, `archived_reason` buat bedain reject eksplisit vs auto-archive-karena-basi, `dedupe_key`); `gsc_settings` (+`memory_thresholds_json` — tempat tunable terpisah dari `opportunity_thresholds_json`, sengaja tidak dicampur; +`last_memory_detection_at`). Sudah pakai pola stored-procedure-guard dari awal (pelajaran dari kegagalan 015/016 di production), bukan `ADD COLUMN IF NOT EXISTS`.
  - **Deteksi pola** (`cms_growth_agent_detect_memory_patterns()`, baru di `growth-agent-service.php`) — beda dari `gsc_opportunities` yang lihat snapshot fetch terbaru, ini `GROUP BY query` atas **seluruh window retensi `gsc_query_data`** (default 90 hari), syarat minimal muncul di beberapa minggu berbeda (`min_distinct_weeks`, default 3) supaya bukan lonjakan sesaat. Trigger: **otomatis-jika-lewat-jadwal** (`cms_growth_agent_detect_memory_if_stale()`, default interval 7 hari — jauh lebih jarang dari fetch GSC 24 jam, karena hasilnya jarang berubah + supaya tidak bikin fatigue review) **+ tombol manual "Analisis Pola"**.
  - **Review UI**: panel inline "Agent Memory" di `growth-agent.php` (bukan halaman terpisah — beda dari GSC Settings yang perlu superadmin-only karena kredensial, Memory tidak nyimpen apa pun sensitif, jadi tier admin+ sama seperti panel "Style rules" yang sudah ada). Sub-bagian Pending Review (Approve/Reject + evidence angka langsung di baris tabel) dan Active (read-only + tombol Archive manual).
  - **Integrasi ke `GrowthAgentPromptBuilder`**: method `buildContext()` sudah menerima `$jobType` sebagai parameter — Memory di-fold cukup dengan extend method itu (`if ($jobType === 'gsc_article_idea')`) lewat method baru `activeMemoryEntries()`, **0 perubahan di titik pemanggilan manapun** (`cms_growth_agent_generate_article_idea()` sudah manggil `buildContext('growth_agent', 'gsc_article_idea')` dari sebelumnya). Ini satu-satunya file dari 3 fase GSC yang akhirnya benar-benar berubah — 2 fase sebelumnya selalu "0 baris berubah" di file ini.
  - **Retensi**: `pending_review` tak direview >30 hari → auto-archive (`archived_reason='stale_pending'`, boleh muncul lagi kalau pola masih ada di deteksi berikutnya). `active` yang `last_confirmed_at`-nya >90 hari (bukan `detected_at` — pola yang masih konsisten tidak pernah basi) → auto-archive (`stale_active`). Reject eksplisit admin (`archived_reason='rejected'`) permanen menekan `dedupe_key` yang sama, tidak pernah dibuat ulang otomatis.
  - **Tidak berubah**: `seo-recommendation-review.php`, `cms_growth_agent_run_seo_recommendation_scan()`, `cms_growth_agent_generate_content_optimization()`, `cms_gsc_compute_opportunities()` — semua di luar jalur `gsc_article_idea`, sesuai keputusan awal (Memory cuma buat ide artikel baru, bukan revisi artikel existing).

**Belum dikerjakan / masih pending:**
- Fase 7: App Promotion module (on hold)
- Migrasi `008_remove_products.sql` belum dijalankan ke database live (disiapkan, tapi eksekusi destructive harus manual oleh user)
- Migrasi `012_cleanup_unused_tables_columns.sql` juga belum dijalankan ke database live — sama-sama destructive/opt-in
- Migrasi `013_remove_livescore_module.sql` (baru) juga belum dijalankan ke database live — destructive/opt-in, jalankan bareng `008`/`012` kalau mau database benar-benar bersih dari tabel yang tidak terpakai
- Migrasi `015_gsc_search_console.sql`, `016_gsc_opportunities.sql`, `017_growth_agent_memory.sql` (baru) belum dijalankan ke database live — non-destructive/idempotent, `015`/`016`/`017` semua sudah pakai pola stored-procedure-guard buat `ADD COLUMN` (bukan `ADD COLUMN IF NOT EXISTS` — lihat catatan MySQL #1064 di `migrations/README.md`), aman kapan saja, tapi tetap perlu eksekusi manual oleh user (atau otomatis lewat auto-migration PHP begitu `gsc-settings.php`/`growth-agent.php` pertama kali dibuka)
- Integrasi GSC belum bisa dipakai sampai: (1) user paste service account JSON + pilih property di `gsc-settings.php`, (2) khusus rekomendasi content optimization & article idea (recommended_agent=`growth_agent`), admin perlu setup agent baru `agent_key=growth_agent` di **AI Agent Settings** dulu (belum ada secara default) — kalau belum di-setup, klik Generate tetap aman (pesan error jelas, bukan crash) tapi rekomendasinya gagal dibuat
- Seluruh alur "Prioritized Opportunities" (§ entri revisi 18 Jul 2026 di atas) belum pernah dites dengan data GSC sungguhan — threshold scoring di `opportunity_thresholds_json` masih nilai estimasi awal, perlu disesuaikan setelah lihat data traffic asli
- **Agent Memory** (§ entri 19 Jul 2026 di atas) butuh beberapa minggu data GSC historis terkumpul dulu (`min_distinct_weeks` default 3 minggu) sebelum pola apa pun realistis terdeteksi — normal kalau awalnya "Analisis Pola" tidak menemukan apa-apa di instalasi yang baru mulai connect ke GSC. Threshold di `memory_thresholds_json` juga masih estimasi awal, sama seperti opportunity thresholds.
